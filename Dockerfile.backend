# 1. Node.js LTS 이미지 사용
FROM node:18

# 2. 필요한 도구 설치 (ffmpeg와 yt-dlp)
RUN apt-get update && \
    apt-get install -y ffmpeg python3 python3-pip && \
    pip3 install -U yt-dlp && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# 3. 작업 디렉토리 설정
WORKDIR /app

# 4. 서버 코드 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

COPY server ./server

# 5. 컨테이너에서 사용할 포트 지정
EXPOSE 5000

# 6. 백엔드 실행
CMD ["node", "server/index.js"]
